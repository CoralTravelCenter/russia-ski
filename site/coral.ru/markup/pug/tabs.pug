include ./mixins

link(href='../../css/tabs.scss' rel='stylesheet' type='text/css')
section.hero.regions-section
	article
		.contenu
			ul.regions-list
				-const coral_regions = coral.regions;
				each region, idx in coral_regions
					li.region-item(data-region = idx)
						div 
							+region-markup(region)
script(type='module' src='../../js/tabs.js')

